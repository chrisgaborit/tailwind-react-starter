import { ModuleType, ModuleLevel, Tone, Language } from './types';

export const moduleTypes = Object.values(ModuleType);
export const moduleLevels = Object.values(ModuleLevel);
export const tones = Object.values(Tone);
export const languages = Object.values(Language);
